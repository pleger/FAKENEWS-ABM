This directory is kept for users who expect the plural `inputs/` name.

The generated workbooks are stored in `../input/`, matching the original SBABM layout. The CLI can load files from either `input/`, `inputs/`, or an explicit path.
